<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:background="@drawable/layout_stroke"
    android:orientation="vertical"
    android:padding="10dp"
    android:paddingBottom="10dp"
    >

    <TextView
        android:id="@+id/tvPostAdi"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentEnd="true"
        android:layout_alignParentTop="true"
        android:layout_toEndOf="@+id/imgKisiUrl"
        android:text=" "
        android:textSize="18sp"
        android:textStyle="bold" />

    <ImageView
        android:id="@+id/imgKisiUrl"
        android:layout_width="64dp"
        android:layout_height="64dp"
        android:layout_alignParentStart="true"
        android:layout_alignParentTop="true"
        app:srcCompat="@drawable/ic_face" />

    <TextView
        android:id="@+id/tvIcerik"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignBottom="@+id/imgKisiUrl"
        android:layout_alignParentEnd="true"
        android:layout_alignStart="@+id/tvPostAdi"
        android:layout_below="@+id/tvPostAdi"
        android:text=" " />


</RelativeLayout>